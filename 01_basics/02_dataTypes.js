"use strict"; //treat all JS code as newer version

// alert("Hello World"); //we are using console.log instead of alert

console.log(3 + 3);  //code readibility should be maintained


console.log(typeof null); // null is an object type in JS, but it represents the absence of value
console.log(typeof undefined); //undefined is a type in JS
